name: manual test
on:
  workflow_dispatch

jobs:
  do-some:
    runs-on: ubuntu-latest
    environment: develop
    # env:
    #   LOCAL_NODE_VERSION: ${{ vars.NODE_VERSION }}
    steps:
      - shell: bash
        env:
          LOCAL_VERSION: $NODE_VERSION
        # run: |
        #   example-command "$LOCAL_VERSION"
          
      - run: echo "1 enviroment $NODE_VERSION"
      # - run: echo "2 enviroment ${{ NODE_VERSION }}"
      - run: echo "3 enviroment ${{ env.NODE_VERSION }}"
      # - name: Read a global variable $NODE_VERSION
      #   run: echo "My global NODE_VERSION value is $NODE_VERSION"

      # - name: read local
      #   run: echo "my local node is $LOCAL_NODE_VERSION"

      # - name: read local 2
      #   run: echo "my local node is ${{ local_node_version }}
      
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: ${{ vars.SECRET_VERSION }}
      # - run: node --version
